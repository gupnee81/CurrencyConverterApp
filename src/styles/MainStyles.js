import styled from 'styled-components';

const PageBody = styled.div`
    margin: 0 auto;
    width: calc(100% - 10%);
    background-color: #fff;
    padding: 5px;
    text-align: left;
`;

export default {
    PageBody,
}

